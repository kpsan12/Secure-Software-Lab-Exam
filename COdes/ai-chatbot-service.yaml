apiVersion: v1
kind: Service
metadata:
  name: ai-chatbot-service
spec:
  type: NodePort
  selector:
    app: ai-chatbot
  ports:
    - protocol: TCP
      port: 5000        # inside pod
      targetPort: 5000  # container port
      nodePort: 30001    # external port
